# ============================================================
# Educare+ - Variáveis de Ambiente para Produção (Docker)
# ============================================================
# Copie este arquivo como .env no servidor de produção:
#   cp .env.production.template .env
# Depois preencha os valores reais de cada variável.
# ============================================================

# --- Servidor ---
NODE_ENV=production
PORT=5000

# --- Banco de Dados (PostgreSQL no container Docker) ---
DB_USERNAME=postgres
DB_PASSWORD=ALTERE_PARA_SUA_SENHA_SEGURA
DB_DATABASE=educareapp
DB_HOST=postgres
DB_PORT=5432
DB_DIALECT=postgres
DB_TIMEZONE=America/Sao_Paulo
DB_SYNC_ENABLED=false

# --- Autenticação JWT ---
JWT_SECRET=ALTERE_PARA_UMA_CHAVE_SECRETA_LONGA_E_ALEATORIA
JWT_EXPIRATION=24h
JWT_REFRESH_SECRET=ALTERE_PARA_OUTRA_CHAVE_SECRETA_DIFERENTE
JWT_REFRESH_EXPIRES_IN=7d

# --- URLs da Aplicação ---
# NOTA: Frontend e Backend usam o MESMO domínio (roteamento por caminho)
# O Traefik direciona /api/* para o backend e o restante para o frontend
FRONTEND_URL=https://educareapp.com.br
BACKEND_URL=https://educareapp.com.br
APP_URL=https://educareapp.com.br
VITE_API_URL=
CORS_ORIGINS=https://educareapp.com.br,https://www.educareapp.com.br,https://n8n.educareapp.com.br

# --- OpenAI ---
OPENAI_API_KEY=sk-COLE_SUA_CHAVE_OPENAI_AQUI

# --- Google Gemini ---
GEMINI_API_KEY=COLE_SUA_CHAVE_GEMINI_AQUI

# --- Qdrant (Vector Database) ---
QDRANT_URL=COLE_SUA_URL_QDRANT_AQUI
QDRANT_API_KEY=COLE_SUA_CHAVE_QDRANT_AQUI

# --- Stripe (Pagamentos) ---
STRIPE_SECRET_KEY=sk_live_COLE_SUA_CHAVE_AQUI
STRIPE_PUBLISHABLE_KEY=pk_live_COLE_SUA_CHAVE_AQUI
STRIPE_WEBHOOK_SECRET=whsec_COLE_SEU_WEBHOOK_SECRET_AQUI

# --- Evolution API (WhatsApp) ---
EVOLUTION_API_URL=https://api.educareapp.com.br
EVOLUTION_API_KEY=COLE_SUA_CHAVE_EVOLUTION_AQUI
EVOLUTION_INSTANCE_NAME=educare-chat

# --- n8n (Automação) ---
N8N_API_KEY=COLE_SUA_CHAVE_N8N_AQUI
N8N_REST_API_KEY=COLE_SUA_CHAVE_N8N_REST_AQUI
EXTERNAL_API_KEY=COLE_SUA_CHAVE_EXTERNAL_API_AQUI

# --- WhatsApp do Owner (para notificações) ---
OWNER_PHONE=5511999999999

# --- Auto-Seed (dados iniciais) ---
# Popula automaticamente subscription_plans e content_items se estiverem vazios
# Defina como "false" para desabilitar
AUTO_SEED_ENABLED=true
